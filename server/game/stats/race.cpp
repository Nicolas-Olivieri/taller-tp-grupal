#include "race.h"


Race::Race(const uint8_t health_factor, const uint8_t mana_factor, const uint8_t recovery_factor):
        health_factor(health_factor), mana_factor(mana_factor), recovery_factor(recovery_factor) {}
